from prophet import run_experiment
from numpy import save

# position_FairPA, FairPA_values = run_experiment(algorithm="FairGeneralProphet", n_experiment_reps=50000, 
#                                                 distribution_type="binomial", n_candidates=1000)
# save('data/FairPA_positions.npy', position_FairPA)
# save('data/FairPA_values.npy', FairPA_values)

# position_FairIID, FairIID_values = run_experiment(algorithm="FairIIDProphet", n_experiment_reps=50000, 
#                                                   distribution_type="binomial", n_candidates=1000)
# save('data/FairIID_positions.npy', position_FairIID)
# save('data/FairIID_values.npy', FairIID_values)

# position_SC, SC_values = run_experiment(algorithm="SC", n_experiment_reps=50000, 
#                                         distribution_type="binomial", n_candidates=1000)

# save('data/SC_positions.npy', position_SC)
# save('data/SC_values.npy', SC_values)

# position_EHKS, EHKS_values = run_experiment(algorithm="EHKS", n_experiment_reps=50000, 
#                                             distribution_type="binomial", n_candidates=1000)

# save('data/EHKS_positions.npy', position_EHKS)
# save('data/EHKS_values.npy', EHKS_values)

# position_CFHOV, CFHOV_values = run_experiment(algorithm="CFHOV", n_experiment_reps=50000, 
#                                               distribution_type="binomial", n_candidates=1000)

# save('data/CFHOV_positions.npy', position_CFHOV)
# save('data/CFHOV_values.npy', CFHOV_values)

# position_DP, DP_values = run_experiment(algorithm="DP", n_experiment_reps=50000, 
#                                         distribution_type="binomial", n_candidates=1000)

# save('data/DP_positions.npy', position_DP)
# save('data/DP_values.npy', DP_values)
